This app is only used to demonstrate calling a Dagger module.

To run:

* `dagger call -m "github.com/mjb141/daggerverse/pytest@main" pytest-with-pip --src-dir . --tests-dir tests/ --requirements-files requirements.txt`
